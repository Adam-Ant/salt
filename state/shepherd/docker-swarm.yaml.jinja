version: "3.9"

services:
  app:
    image: containrrr/shepherd
    environment:
      APPRISE_SIDECAR_URL: "{{ notification_url }}"
      IGNORELIST_SERVICES: "label=shepherd.enable=false"
      TZ: 'Europe/London'
      SLEEP_TIME: '6h'
      FILTER_SERVICES: ''
      VERBOSE: 'true'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    deploy:
      placement:
        constraints:
          - node.role == manager
